import 'package:flutter/material.dart';

class BhajanMusicScreen extends StatelessWidget {
  const BhajanMusicScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
